namespace :greeting do desc 'outputs hello to the terminal'
 task :environment do
  require_relative './config/environment'
    task :hello do
      puts "hello from Rake!"
  desc 'outputs hola to the terminal'
    task :hola do
       puts "hola de Rake!"
       
end
